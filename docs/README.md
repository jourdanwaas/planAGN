Generating the docs
----------

Use [mkdocs](http://www.mkdocs.org/) structure to update the documentation.

Build locally with:

```sh
uv run mkdocs build
```

Serve locally with:

```sh
uv run mkdocs serve
```
